#include "main.h"
/**
 * _isalpha - checks alphabetic character
 * @m: rep character
 * Return: 1 if letter,lower or uppercase; 0 for otherwise
 */
int _isalpha(int m)
{
	if ((m >= 'A' && m <= 'Z') || (m >= 'a' && c <= 'z'))
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
